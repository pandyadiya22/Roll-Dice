# Roll-Dice
A simple Python-based digital dice simulator that generates random numbers between 1 and 6 using the random module. It displays interactive reactions based on the rolled number and runs in a continuous loop until the user stops, demonstrating basic functions, loops, dictionaries, and user input handling.
